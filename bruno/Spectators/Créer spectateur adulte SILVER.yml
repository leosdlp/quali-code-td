info:
  name: Créer spectateur adulte SILVER
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}/spectators"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Alice Martin",
        "email": "alice@example.com",
        "birthDate": "1990-04-12",
        "loyaltyTier": "SILVER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
