info:
  name: Lister les sessions
  type: http
  seq: 5

http:
  method: GET
  url: "{{api_url}}/sessions"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
