info:
  name: Lister les spectateurs
  type: http
  seq: 4

http:
  method: GET
  url: "{{api_url}}/spectators"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
