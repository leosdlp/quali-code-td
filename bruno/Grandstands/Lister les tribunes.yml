info:
  name: Lister les tribunes
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/grandstands"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
