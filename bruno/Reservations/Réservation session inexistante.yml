info:
  name: Réservation session inexistante
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/reservations"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "spectatorId": 1,
        "grandstandId": 1,
        "sessionIds": [
          999
        ],
        "seatCount": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
