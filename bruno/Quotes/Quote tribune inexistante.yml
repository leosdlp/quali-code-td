info:
  name: Quote tribune inexistante
  type: http
  seq: 5

http:
  method: POST
  url: "{{api_url}}/reservations/quote"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "grandstandId": 999,
        "sessionIds": [
          1
        ],
        "seatCount": 2,
        "spectatorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
