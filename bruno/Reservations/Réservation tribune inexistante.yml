info:
  name: Réservation tribune inexistante
  type: http
  seq: 5

http:
  method: POST
  url: "{{api_url}}/reservations"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "spectatorId": 1,
        "grandstandId": 999,
        "sessionIds": [
          1
        ],
        "seatCount": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
