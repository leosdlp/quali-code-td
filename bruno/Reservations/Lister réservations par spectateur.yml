info:
  name: Lister réservations par spectateur
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/reservations?spectatorId=1"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: spectatorId
      value: 1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
