info:
  name: Quote session inexistante
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/reservations/quote"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "grandstandId": 1,
        "sessionIds": [
          999
        ],
        "seatCount": 2,
        "spectatorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
