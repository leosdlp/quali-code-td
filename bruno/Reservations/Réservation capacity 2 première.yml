info:
  name: Réservation capacity 2 première
  type: http
  seq: 8

http:
  method: POST
  url: "{{api_url}}/reservations"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "spectatorId": 1,
        "grandstandId": 2,
        "sessionIds": [
          3
        ],
        "seatCount": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
