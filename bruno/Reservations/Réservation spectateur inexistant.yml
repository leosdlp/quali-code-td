info:
  name: Réservation spectateur inexistant
  type: http
  seq: 4

http:
  method: POST
  url: "{{api_url}}/reservations"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "spectatorId": 999,
        "grandstandId": 1,
        "sessionIds": [
          1
        ],
        "seatCount": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
