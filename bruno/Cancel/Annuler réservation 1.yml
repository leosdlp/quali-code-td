info:
  name: Annuler réservation 1
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}/reservations/1/cancel"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
