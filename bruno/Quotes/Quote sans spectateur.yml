info:
  name: Quote sans spectateur
  type: http
  seq: 4

http:
  method: POST
  url: "{{api_url}}/reservations/quote"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "grandstandId": 1,
        "sessionIds": [
          3
        ],
        "seatCount": 2,
        "spectatorId": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
