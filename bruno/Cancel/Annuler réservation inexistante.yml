info:
  name: Annuler réservation inexistante
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/reservations/999/cancel"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
