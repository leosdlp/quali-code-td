info:
  name: Créer spectateur jeune GOLD
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/spectators"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Leo Martin",
        "email": "leo@example.com",
        "birthDate": "2014-04-12",
        "loyaltyTier": "GOLD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
