info:
  name: Créer spectateur invalide
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/spectators"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "",
        "email": "email-invalide",
        "birthDate": "2030-01-01",
        "loyaltyTier": "SILVER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
