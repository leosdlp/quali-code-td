info:
  name: Annuler réservation 1 une deuxième fois
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/reservations/1/cancel"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
