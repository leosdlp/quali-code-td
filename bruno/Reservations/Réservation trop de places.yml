info:
  name: Réservation trop de places
  type: http
  seq: 7

http:
  method: POST
  url: "{{api_url}}/reservations"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "spectatorId": 1,
        "grandstandId": 1,
        "sessionIds": [
          1
        ],
        "seatCount": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
